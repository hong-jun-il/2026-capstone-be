name: CI/CD Deployment

on:
  push:
    branches: ['main']

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Backend Image
        uses: docker/build-push-action@v5
        with:
          context: ./modu-bot-be
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/modu-bot-be:latest

      - name: Build and Push AI Image
        uses: docker/build-push-action@v5
        with:
          context: ./modu-bot-ai
          file: ./modu-bot-ai/dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/modu-bot-ai:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/project
            docker-compose -f docker-compose.prod.yml pull
            docker-compose -f docker-compose.prod.yml stop backend ai
            docker-compose -f docker-compose.prod.yml rm -f backend ai
            docker-compose -f docker-compose.prod.yml up -d
            docker image prune -f
