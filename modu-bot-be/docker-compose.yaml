services:
  postgres:
    image: postgres:14
    container_name: moduBot-db
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    env_file:
      - .env.local
